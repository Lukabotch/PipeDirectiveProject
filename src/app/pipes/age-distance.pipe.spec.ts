import { AgeDistancePipe } from './age-distance.pipe';

describe('AgeDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new AgeDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
